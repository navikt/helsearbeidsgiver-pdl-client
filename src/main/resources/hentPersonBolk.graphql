query($identer: [ID!]!) {
     hentPersonBolk(identer: $identer) {
        ident
        person {
            navn {
                fornavn
                mellomnavn
                etternavn
            }
            foedselsdato { foedselsdato }
            adressebeskyttelse { gradering }
        }
        code
        }
}
